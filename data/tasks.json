[
  {
    "task_id": "syntax_missing_comma",
    "difficulty": "Easy",
    "bug_type": "Logic Bug",
    "description": "This query should retrieve all users from the database but only returns half the results. The WHERE clause is incorrectly filtering rows. Fix the query to return all users with their id, username, and email.",
    "broken_query": "SELECT id, username, email FROM users WHERE id <= 25;",
    "gold_query": "SELECT id, username, email FROM users;",
    "max_steps": 8,
    "hints": [
      "Check how many rows the query returns versus how many users exist.",
      "A query that returns all users should not need a WHERE clause at all."
    ]
  },
  {
    "task_id": "syntax_ambiguous_column",
    "difficulty": "Easy",
    "bug_type": "Syntax Bug",
    "description": "The query joins users and orders but uses the wrong table prefix on the id column. It should return each user's username alongside their order id, but currently returns the user id instead. Fix the table qualifier so the correct id is selected.",
    "broken_query": "SELECT users.username, users.id FROM users JOIN orders ON users.id = orders.user_id;",
    "gold_query": "SELECT users.username, orders.id FROM users JOIN orders ON users.id = orders.user_id;",
    "max_steps": 8,
    "hints": [
      "Check which table each column in the SELECT list belongs to.",
      "The goal is to return the order id, not the user id.",
      "Both tables have an 'id' column \u2014 make sure you prefix id with the correct table name."
    ]
  },
  {
    "task_id": "logic_operator_precedence",
    "difficulty": "Medium",
    "bug_type": "Logic Bug",
    "description": "This query is supposed to return only high-value cancelled or pending orders, but due to operator precedence the AND binds tighter than OR and the condition evaluates incorrectly. Add parentheses around the OR condition to fix the logic.",
    "broken_query": "SELECT id, user_id, status, total_amount FROM orders WHERE status = 'cancelled' OR status = 'pending' AND total_amount > 500.00 ORDER BY total_amount DESC;",
    "gold_query": "SELECT id, user_id, status, total_amount FROM orders WHERE (status = 'cancelled' OR status = 'pending') AND total_amount > 500.00 ORDER BY total_amount DESC;",
    "max_steps": 12,
    "hints": []
  },
  {
    "task_id": "logic_date_boundary",
    "difficulty": "Medium",
    "bug_type": "Logic Bug",
    "description": "This query should return all orders from a specific date onwards but the row count does not match expectations. Fix it.",
    "broken_query": "SELECT id, user_id, status, total_amount, created_at FROM orders WHERE DATE(created_at) > '2025-02-15' ORDER BY created_at;",
    "gold_query": "SELECT id, user_id, status, total_amount, created_at FROM orders WHERE DATE(created_at) >= '2025-01-01' ORDER BY created_at;",
    "max_steps": 12,
    "hints": [
      "Check the row count — the query should return all orders for the entire year.",
      "Both the comparison operator AND the date value may need to be changed."
    ]
  },
  {
    "task_id": "perf_n_plus_one",
    "difficulty": "Hard",
    "bug_type": "Performance Bug",
    "description": "This query returns correct results but becomes extremely slow as the products table grows. Fix the performance issue.",
    "broken_query": "SELECT id, name, (SELECT SUM(quantity) FROM order_items WHERE product_id = products.id) AS total_sold FROM products;",
    "gold_query": "SELECT products.id, products.name, COALESCE(SUM(order_items.quantity), 0) AS total_sold FROM products LEFT JOIN order_items ON products.id = order_items.product_id GROUP BY products.id, products.name;",
    "max_steps": 15,
    "hints": [
      "Consider how many times the database engine executes work for each row in the result."
    ]
  },
  {
    "task_id": "logic_window_partition",
    "difficulty": "Hard",
    "bug_type": "Logic Bug",
    "description": "This query ranks products by price but the rankings appear incorrect. Fix the query.",
    "broken_query": "SELECT p.id, p.name, p.price, c.name AS category, RANK() OVER (ORDER BY p.price DESC) AS price_rank FROM products p JOIN categories c ON p.category_id = c.id ORDER BY c.name, price_rank;",
    "gold_query": "SELECT p.id, p.name, p.price, c.name AS category, RANK() OVER (PARTITION BY p.category_id ORDER BY p.price DESC) AS price_rank FROM products p JOIN categories c ON p.category_id = c.id ORDER BY c.name, price_rank;",
    "max_steps": 15,
    "hints": [
      "The rankings look correct globally but may not reflect the intended grouping."
    ]
  },
  {
    "task_id": "logic_missing_having",
    "difficulty": "Hard",
    "bug_type": "Logic Bug",
    "description": "This query should return only users who have placed more than one order but it returns too many rows. Fix it.",
    "broken_query": "SELECT o.id AS order_id, u.username, COUNT(oi.id) AS item_count, SUM(oi.quantity * oi.unit_price) AS order_total FROM orders o JOIN users u ON o.user_id = u.id JOIN order_items oi ON o.id = oi.order_id GROUP BY o.id, u.username ORDER BY item_count DESC;",
    "gold_query": "SELECT o.id AS order_id, u.username, COUNT(oi.id) AS item_count, SUM(oi.quantity * oi.unit_price) AS order_total FROM orders o JOIN users u ON o.user_id = u.id JOIN order_items oi ON o.id = oi.order_id GROUP BY o.id, u.username HAVING COUNT(oi.id) > 1 ORDER BY item_count DESC;",
    "max_steps": 15,
    "hints": [
      "The query returns more rows than expected. Think about where result filtering happens after GROUP BY.",
      "You need a HAVING clause. The condition should filter for orders with more than one item."
    ]
  },
  {
    "task_id": "cascade_pipeline_bug",
    "difficulty": "Hard",
    "bug_type": "cascade",
    "description": "A 3-step reporting pipeline is producing wrong revenue figures. The pipeline calculates per-category revenue using CTEs. Each step depends on the previous. Fix all broken steps so the final result matches the expected output.",
    "broken_query": "WITH\nstep1 AS (\n    SELECT product_id, SUM(quantity) as total_qty\n    FROM order_items\n    GROUP BY order_id\n),\nstep2 AS (\n    SELECT p.category_id, s.total_qty, p.price\n    FROM products p\n    JOIN step1 s ON p.id = s.product_id\n),\nstep3 AS (\n    SELECT category_id, SUM(total_qty) as units_sold, SUM(total_qty * price) as revenue\n    FROM step2\n    GROUP BY category_id\n)\nSELECT c.name as category, s.units_sold, s.revenue\nFROM step3 s\nJOIN categories c ON s.category_id = c.id\nORDER BY s.revenue DESC;",
    "gold_query": "WITH\nstep1 AS (\n    SELECT product_id, SUM(quantity) as total_qty\n    FROM order_items\n    GROUP BY product_id\n),\nstep2 AS (\n    SELECT p.category_id, s.total_qty, p.price\n    FROM products p\n    JOIN step1 s ON p.id = s.product_id\n),\nstep3 AS (\n    SELECT category_id, SUM(total_qty) as units_sold, SUM(total_qty * price) as revenue\n    FROM step2\n    GROUP BY category_id\n)\nSELECT c.name as category, s.units_sold, s.revenue\nFROM step3 s\nJOIN categories c ON s.category_id = c.id\nORDER BY s.revenue DESC;",
    "max_steps": 20,
    "hints": [
      "Start by understanding what each step of the pipeline is supposed to produce before attempting any fix.",
      "Run step1 in isolation first — does GROUP BY order_id make sense for computing per-product quantity totals?",
      "The bug is in step1. Once step1 is wrong, every downstream step inherits the error."
    ]
  }
]